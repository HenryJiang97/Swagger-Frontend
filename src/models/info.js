class Info {
    constructor(title, version, description, basePath, host) {
        this.title = title;
        this.version = version;
        this.description = description;
        this.basePath = basePath;
        this.host = host;
    }
}

export {
    Info
};