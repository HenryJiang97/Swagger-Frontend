export default class Parameter {
    constructor(name, description, type, format) {
        this.name = name;
        this.description = description;
        this.type = type;
        this.format = format;
    }
}